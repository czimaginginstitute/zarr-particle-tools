# Validates PR title follows conventional commits
name: conventional-commits
permissions:
  contents: read

on:
  pull_request:
    branches: [main]
    types:
      - edited
      - opened
      - synchronize
      - reopened

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  conventional_commit_title:
    runs-on: ubuntu-latest
    steps:
      - uses: chanzuckerberg/github-actions/.github/actions/conventional-commits@e8f79697b959811b00e07565c488cf4a64f428e3
